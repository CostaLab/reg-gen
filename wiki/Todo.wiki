= TO-DO =
This page contains information on functions that should be implemented.

== GenomicRegion ==

|| *Developer* || *Function* || *Input* || *Output* || *Description* ||
|| || - || - || - || Implement bed fields: score, thickStart, thickEnd, itemRgb (explanation [http://bit.ly/1hdVNjA here]) ||

== GenomicRegionSet ==

|| *Developer* || *Function* || *Input* || *Output* || *Description* ||
|| || general_sort || (GenomicRegion attribute) `*`fields || GenomicRegionSet || Sorts GenomicRegionSet by a defined order of fields ||

== CoverageSet ==

|| *Developer* || *Function* || *Input* || *Output* || *Description* ||
|| || || || || ||

== ExperimentalMatrix ==

|| *Developer* || *Function* || *Input* || *Output* || *Description* ||
|| || || || || ||

== GeneSet ==

|| *Developer* || *Function* || *Input* || *Output* || *Description* ||
|| || || || || ||

== GeneMapping ==

|| *Developer* || *Function* || *Input* || *Output* || *Description* ||
|| || gene_coord_association || GenomicRegionSet coordinates<br>teste || || ||

== Util ==

|| *Developer* || *Function* || *Input* || *Output* || *Description* ||
|| || || || || ||